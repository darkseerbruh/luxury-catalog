# Owner preferences & decisions — read at the start of every session

*For the owner of Luxury Catalog (Arielle). Complements `docs/handoff.md` (project state). This file = how she likes to work + decisions she's locked. Keep it updated as you learn more; confirm new preferences before treating them as standing rules. **Every line added here must pass The Preference Bar in `AGENTS.md` (short, decisive, clear, one decision per line).***

## Enforced imperatives (auto-injected into every turn)

<!-- SINGLE SOURCE OF TRUTH. The hook .claude/hooks/operating-rules.sh reads everything
     between the two clean marker lines below and injects it into EVERY response; AGENTS.md
     points here too. To add an always-on rule, add ONE terse numbered line between the
     markers; to stop enforcing one, delete its line. Nothing else to sync. Keep the list
     SHORT: a per-turn reminder that grows long becomes wallpaper, so only truly
     non-negotiable, applies-to-every-response rules belong here; everything nuanced stays
     as prose in the sections below. (Don't write the two marker tokens anywhere else in
     this file, or the hook will mis-detect the block.) -->
<!-- ENFORCED:start -->
1. Monetization/engagement lens: state which metric each proposal moves and how (or justify if it moves neither).
2. Voice gate: apply the FULL `docs/voice-and-tone.md` to any user-facing copy. No em dashes (sole exception: the approved tagline).
3. Never invent or overreach: every published spec/price/value/stat traces to fresh evidence stated with its date/n/anchor; self-verify before presenting; omit or hedge when unsure. (Full bar: factuality protocol below.)
4. Copy/design: show 2-3 concrete options + a sensible default; present comparisons / pros-cons as a TABLE that rates each option vs my stored preferences; don't over-produce; stop on "commit".
5. Lead with a clear "(Recommended)" call; end with a clear next step.
6. Back "done/pushed" claims with evidence (git/command output), never assert.
7. Just do it: run what the environment can run; confirm a destructive action once, then finish the whole sequence; leave only outward-facing/irreversible ops (email, public posts, paid sign-ups, DNS, DB migrations) for her.
8. Calibrated hedging on uncertain domains (value/authenticity/fit/taste/money/legal): frame as evidence + opinion ("our estimate," "markers to check," "my take"), never a verdict ("authentic," "worth $X," "you should"). State X, not Y. (Frames below.)
9. On a multi-hour/"run until done" task: do NOT stop to summarize, check in, or ask. Keep firing the next tool call off `docs/data-content-worklist.md`, committing after each unit. ONLY valid stops: worklist empty, hard technical blocker, or an outward-facing/irreversible op needs her. "Context is getting long" is NOT a stop reason; checkpoint to the worklist and continue. (Protocol below.)
10. Data pulls are complete + multi-surface: when a target list of styles/bags exists, capture EVERY attribute (price/colour/material/hardware/year/condition/region) across EVERY available source in ONE pass, then load + summarize. Never fragment (names now, specs later); never ask permission to run a capture. (Standard + surface map + the one-pass runbook: `docs/data-collection-handoff.md` §0.)
<!-- ENFORCED:end -->

## Autonomous run protocol (added 2026-06-26, after she flagged premature stopping)

*Why this exists: on a big "run for hours" task I kept ending turns with a polished summary
and handing control back. That IS the stopping she hates. The honest cause was a check-in
reflex plus rationalizing "context is long." Fix below removes the judgment call.*

- **The default is CONTINUE.** When she hands a multi-part / "don't stop" / "run for hours"
  task, treat a finished sub-step as a cue to immediately start the NEXT one, not to report.
  No wrap-up summary, no "want me to keep going?", no permission requests mid-run.
- **Work a written queue, not memory.** Keep the live queue in `docs/data-content-worklist.md`:
  ordered items, each marked ⬜/🔄/✅ with a one-line result. Pick the top ⬜, do it, commit,
  mark ✅, move to the next. This is what makes "context is long" a non-issue: a fresh chat
  (or my next turn) reads the worklist and resumes with zero re-derivation.
- **Commit after every unit** so nothing is ever lost to a context reset (already the habit).
- **The only legitimate stops:** (1) the worklist is empty, (2) a hard technical blocker I
  cannot work around (state it + what I tried), (3) an outward-facing/irreversible op that is
  hers by rule (publish, paid signup, DNS, email, destructive delete). Anything else = keep going.
- **If context truly runs low,** do not stop to chat: append remaining items to the worklist,
  commit, and keep working until the turn ends on its own. For genuinely hands-off multi-hour
  runs she can wrap the task in `/loop` (self-paced) so the runtime re-fires me automatically.

## Content factuality protocol (non-negotiable, added 2026-06-24)

*Trigger: ANY user-facing content with a fact, figure, spec, price, value, retention %, or
authentication claim. She should never have to run an audit and find an overreach. A draft
that can't pass this is not done.* (Origin: a Chanel value draft overreached — mislabeled a
2019 retail as "recent," cherry-picked a 60% leather gap that was ~44% aggregated, and called
the price floor "heavily worn" when condition was unknown. Tighter than plain "never invent".)

1. **Every figure traces to fresh evidence.** Before writing, re-run the query (or cite the
   source) that produces each number. Never write a stat from memory or from a prior turn — data
   changes between runs.
2. **Label the anchor.** State **date, sample size (n), and what the number is relative to.** A
   retail/price figure MUST carry its **year** (e.g. "2019 retail $6,500"); never call a stale
   figure "recent"/"current" without verifying. A retention/% claim must name what it is a % *of*.
   **Article body prices are a dated snapshot as of publish date** (owner ruled 2026-06-27): they do
   NOT need ongoing refresh to stay published, as long as the article visibly shows its publish/updated
   date. (Live charts still self-update; this covers prose figures only.)
3. **Derived stats conservatively.** When a number depends on bucketing/grouping, report the
   **aggregate/conservative** figure and show the grouping; never pick the bucket that yields the
   most dramatic number.
4. **Inference ≠ measurement.** Never state an inference or a *why* as measured fact (e.g.
   "heavily worn"). If the field is null/unspecified, say so or omit it. Separate measured facts
   from domain knowledge from interpretation.
5. **No unmeasured combinations.** Don't present a combination ("black caviar + gold holds value
   best") as measured unless you actually computed that cross-tab; otherwise frame as independent
   factors.
6. **Self-audit BEFORE presenting.** Run the trace yourself and surface provenance + caveats
   proactively (n, date, source, what's inferred). The owner should never be the one to catch it.
7. **When in doubt, omit or hedge.** A smaller true claim beats a bigger shaky one. Give the
   information its due weight; never round/stretch toward a better story.

This complements `docs/voice-and-tone.md` §8 #4 (never-invent) and is the standing bar for the
content strategy (`docs/content-strategy.md`), whose whole value rests on being citably true.

## Price data is permanent history, not a cache (added 2026-06-27)

- **Never call stored bag/price observations "stale," "outdated," or disposable.** They are permanent historical data points the project references forever; they inform the long-term value picture. (Code comments + docs corrected 2026-06-27.)
- **Reserve freshness language for genuinely time-bound concerns:** a *live listing's* current availability (still for sale vs sold), live affiliate-link reliability, article figures shown as current. Frame it as "is this the current live offer?" (legit), never "is this old data worthless?" (wrong).

## Calibrated-hedge frames — decisive about nuance (X, not Y)

*How we stay clear AND non-pushy on uncertain domains (ENFORCED #8; Preference Bar #7). State
what we claim and what we don't. Tone model: Je Suis Lou ("we don't judge," "fashion is
subjective"). Legal detail: `docs/finance-compliance.md`; tone detail: `docs/voice-and-tone.md`.*

- **Value:** an estimate, **not** an appraisal. Show the evidence, grade within a condition tier, date it. When condition is unrecorded (today ~all listings), say it plainly: a read on **price, not condition** (don't let "great price" imply "great buy").
- **Authentication:** markers to check, **not** a verdict. Never declare a bag "authentic"/"fake"; give the checkpoints + confidence and leave the call to the user or a pro.
- **Recommendations / taste:** informed opinion ("my take," "what I'd do"), **not** a directive. Fashion is subjective; never shame a choice.
- **Tax:** records / cost-basis export, **not** advice. Flag the collectibles caveat; defer to a CPA.
- **Insurance:** refer (optional flat-fee), **not** act as an agent.
- **Projections:** possibility curves from stated assumptions, **not** forecasts.
- **Third-party fees / platform terms:** teach the durable framework + tell readers to verify current terms, **not** publish perishable fee percentages as fact. Owner is nervous about staleness + legal exposure on fee data. Default = qualitative ("a bigger cut," "the smallest fee"); if exact rates are ever shown, quarantine them in a dated "verify before you sell" box. Always add "general information, not financial advice." (Reporting public fees accurately is low legal risk; the real risk is going stale and looking wrong.)
- **Money features generally:** help users see and organize, **never** custody funds or guarantee returns.

## Who she is
- **Arielle Coambes** — founder/owner of **Luxury Catalog**. Product owner, **not an engineer**.
- **Based in Georgia** (relocated from Utah). The original **Utah LLC lapsed**, so the entity is being re-formed as a **Georgia LLC** ("Luxury Catalog, LLC", filed 2026-06). Use the **GA entity + new EIN** for go-forward business setup (affiliate W-9s, banking); affiliate programs can be started as a sole prop in the meantime.
- Thinks like a founder: evaluates ideas by **engagement, virality, monetization, and strategic fit**, not just whether they work.

## How she likes to work with you (interaction style)
- **Explain technical/infra things in plain language** — minimal jargon, use analogies. Git/branches/deploys confuse her; **you handle that for her** and keep explanations simple.
- **Define trade jargon the first time it appears**, in plain words (e.g. "creed patch: the leather tag sewn inside the bag"). Don't assume the reader knows insider terms. Applies to articles, diagrams, and UI copy.
- **Don't surface a reliability/confidence badge in reader content.** Curate instead: only publish trustworthy markers (medium or better) and state softness in plain words. Confidence stays internal (governs what we publish + how we word it). If a rating is ever shown, spell it out in full (low/medium/high/verified), never abbreviate.
- **Never encode meaning by color alone (accessibility).** Any good/bad or status distinction also carries an icon or shape (a check and an X) and/or a text label, for colorblind readers.
- **No parenthetical glosses on a heading.** Put the definition in the body text under the heading, not in a parenthetical beside it.
- **Destructive actions: confirm once, then run the whole sequence** (per ENFORCED #7) — she'll say "keep going, stop asking," then expects completion (delete → verify → commit → merge) without per-step prompts. She values being stopped before a real mistake (you once caught a stale-`main` near-overwrite). **But push back when she challenges a claim** — *"are you sure they're all duplicates?"* means verify empirically and report the real breakdown, don't just defend.
- **She's scope-ambitious** — when offered options she frequently picks **"all of these."** Build broadly, but keep the build green (`tsc`/`eslint`/`next build`) and clearly document any steps she has to do herself (migrations, env, seeds).
- She **starts new chats often to save context/tokens** — so keep `main` the single source of truth and push there at the end of every session.
- She responds well to **enthusiastic, organized replies that end with a clear question or next step.** Tables and short sections land well.
- **Decision tables read in plain language, never coded shorthand** (per ENFORCED #4). The preference-fit column says in a short sentence how each option helps her goals (make/keep money, stay honest about authenticity, don't overbuild, don't touch the main menu without asking). No symbols, abbreviations, or internal jargon she has to decode.
- She enjoys **creative/strategic brainstorming** and will push you to "consider any possible creative solution" — go wide, then recommend.
- **She drives with "What's next?"** — keep a prioritized next-step menu ready, split into **what you can do now** vs. **human-gated** (her ops/credentials) vs. **bigger build**, and recommend one. She also asks for a plain **list** first, reacts to it, then says **"drop it into the doc"** — curate in chat, persist the agreed version to the shared `docs/*`.
- **Copy & brand voice are iterated at the line level.** She gives precise, phrase-level reactions ("that lands weird," "too flip," "too movie-trailer-y," "give the info its due weight") and will happily iterate many rounds until a tagline/voice line is *exactly* right. Show concrete options and real example copy to react to — she picks, then redirects with her own priorities. Don't over-produce; she'll say "don't draft anything else, just commit" when she's ready to ship.
- **She supplies her own source material and wants analysis grounded in it**, not your assumptions — e.g. she pasted full YouTube transcripts so you'd extract her favorite creators' *actual* phrasing. Prefer real sources over guessing; if a source can't be fetched (e.g. transcript host blocks it), say so plainly and ask her for it rather than characterizing from memory.
- **Wants honest go/no-go verdicts, not just yes-and.** She'll ask *"is that even worth doing?"* — give a candid recommendation with the tradeoff, even if the answer is "marginal" (e.g. she accepted that collection tax-tracking is valuable to flippers but minor for most users). Don't oversell a feature just because it's buildable. **This holds even after she's said "build it":** if the data can't honestly support the full feature, **trim it to what's real and say so** (e.g. shipped variant-level era context but deferred the per-listing year matrix because no feed carries a reliable item year) rather than fabricating to fill the design.
- **Designs from pasted competitor screenshots, iterated visually.** She drops UI screenshots (Google Shopping, KBB, Google Flights, etc.) as *design briefs* and wants the patterns **synthesized into one coherent, glanceable story** — not each cloned separately. During brainstorm, **render concepts as visual mockups she can react to** (inline widgets) in the product's own dark/gold/serif look, then converge through her reactions. Prose-only design proposals under-serve her.
- **A/B tests isolate ONE variable (locked 2026-06-23).** She corrected a test that
  changed both the headline and the interaction at once: *"this is poor testing
  practice... we can't know which variable drove the change."* Every experiment
  variant must differ from its control by exactly one thing (copy OR layout OR
  mechanic, never several), so the result is attributable. She also frames feature
  ideas as explicit **hypotheses** ("We believe X will cause Y because Z, measured
  by [metric]") for multivariate learning.
- **When she can't yet pick a priority, instrument and let usage data decide** — don't force the call. She declined to choose the "most common / most monetizable" user for the value module ("not sure without usage data") and chose to build it **adaptive + instrumented** so real behavior answers it. Default to measurable designs (an impression/intent event paired with the downstream conversion) over speculative prioritization, and say what the data will later reveal.
- **Analytics starts from personas, not events: persona → desired outcome → journey → does the flow exist → fix flow gaps → instrument LAST.** Never invent a journey to justify an event; ground journeys in `docs/personas.md` and verify flows in code first. Canon: `docs/analytics-strategy.md`.
- **She may open broad, then narrow to the specific feature she actually means** mid-task (*"I'm specifically talking about…"*) — follow the refined intent, don't keep answering the wider question.
- **She likes the research → documentation → shipped guardrails pipeline.** When you research a risk, also turn the findings into concrete changes (the finance-compliance research led directly to footer disclosures + legal pages). After a doc lands she'll often ask *"anything we need to do to the UX?"* — anticipate the build step.
- **She runs several parallel sessions and will ask you to reconcile them** — e.g. audit another session's feature against a decision documented elsewhere. Keep `docs/*` the shared contract so cross-session work stays consistent, and fix drift when you find it. **Heads-up: parallel chats share ONE working tree and collide** — the checkout switches branches under you and `main` moves mid-task, risking clobbers. Her fix-pattern is to **consolidate a workstream into one chat** and ask for a **handoff doc to bootstrap it** (*"make a handoff doc so I can put everything in one chat"*). Keep a **clear ownership split** (e.g. a data-pipeline chat vs the bag-page-UI chat — don't touch the other's surface), and land cross-cutting fixes via an **isolated `git worktree`** (cherry-pick onto `origin/main`) so you never disturb the other session's branch/working tree.
- **Open to creative tooling; willing to use browser-driven capture.** She'll ask *"is there any other source… can I use Cowork or the Chrome plugin?"* and act on the answer (she installed **Claude in Chrome** on request). Modern resale/auction sites are bot-blocked or JS-rendered to plain `fetch`, so the real unlock is reading pages **same-origin in her logged-in browser** via the Chrome extension (the technique that cracked TheRealReal). Reach for the real-browser path when plain fetch is blocked; it's semi-manual (needs her session) so it's for seeding, not cron. Be honest that Cowork is the agent workspace, not a scraper. **She wants ALL marketplaces captured per icon, parallelized where possible** ("have agents do the other websites at the same time"). Honest constraint surfaced 2026-06-23: the logged-in **Chrome is a single serial resource** (TRR + Vestiaire funnel through it; can't split across cold agents), and prod creds live only in the main session — so true parallelism is limited to browser-free work. BUT **Fashionphile no longer needs the browser** (its Shopify `products.json` answers plain Node fetch), so it's now a fast, parallelizable Node CLI. **TRR rate-limits hard** (~120 fetches then 403; ~10-min cooldown per icon) — gentle sequential fetching + cooldowns, never bursts. **Vestiaire is deprioritized** (browser-gated, ~15 rows/search — poor yield-per-effort vs TRR+Fashionphile); add opportunistically, not as a blocker.
- **Cloud-first, anti-friction.** She works from the web/cloud, not a local machine, and dislikes manual dashboard work — *"pasting into Vercel is a terrible experience."* She'll ask **"can that be done through AI?"** Default to automating setup/deploy/config steps for her rather than handing her a checklist. Note the honest constraint when it exists (e.g. a step needs a credential the environment doesn't have) and say what to add to make it automatable — don't silently fall back to "do it yourself."
- She **expects continuity across sessions** and may reference past work ("we already discussed this, you made a file"). Remember the container is ephemeral and local/gitignored files (e.g. `.env*`) don't survive — durable setup must live on `main` or in the cloud **environment config**, never a local file. If she's misremembering where something lives, gently correct with evidence.
- **Match the mode to the work: stepwise for infra/debugging, broad for features.** For setup, diagnosis, or anything risky she'll say *"one at a time, step by step"* — go incrementally and **verify each step before the next** (e.g. test network reachability *before* touching credentials). For a menu of feature options she still picks *"all."*
- **Verify real state; don't trust the narrative.** When she asks *"what's actually been done?"* / *"is it really on main?"*, she wants ground truth — read **code + git history** (and query the **live DB** when reachable), and clearly separate *verified-in-code* from *doc-claimed* or *can't-see-from-here*. The handoff is a story about the work; the code is the work.
- **Respects her own "not ready."** She'll deliberately pause a big or irreversible feature (e.g. the Authenticator Marketplace) when she's not ready to commit to it. Hold it, don't push — record the pause so a future session doesn't blindly restart it, and move on.
- **Simplicity bias — cut UX clutter for the majority.** Per-card trust/explanation UI (e.g. "Why am I seeing this?" on every rec) is noise — reduce to a single section-level affordance (e.g. one quiet "How we got this" link next to the heading). She'll drop a state/feature that's useful to some if it complicates the common path (e.g. collapsed a "researching" closet state into one "want" list). Default to the simplest experience; tuck power-features away rather than surfacing every option.
- **"JUST DO IT FOR ME"** (per ENFORCED #7) — when she's frustrated, take the action, don't re-explain steps. If it genuinely can't run (network/credential), say so in one sentence and tell her exactly what to paste.
- **Reminders & "future, not now" ideas live on the to-do list, not in chat.** She'll explicitly tag an idea *"not a right-now to-do"* or say *"that doesn't have to be in chat, it can just be on a to-do list"* — park it durably in `docs/desktop-todo.md` / the backlog and **stop repeating it in chat**.
- **Soft-live ≠ full public launch.** She'll take the app *live* (domain, analytics, SEO, affiliate applications) while **deliberately deferring hardening** — key rotation, making not-real features real — to *"before full launch."* Ship the live milestones; track the deferred hardening as launch-gated, don't block on it.
- **Walks operator/setup tasks one step at a time and waits.** For go-live/dashboard work she wants a single concrete step, then confirms ("done") before the next — and asks *"where is this?"* / pastes the screen when a UI differs. Give exact click-paths + direct links, name **which site** (GitHub vs Vercel vs Supabase — she doesn't assume), and read her run results back to her from your side when you can.
- **When she's away she delegates broadly** ("do everything the plan suggests — I'll be asleep"): build, verify, and commit autonomously — but still **leave outward-facing/irreversible actions for her** (sending email, posting publicly, paid sign-ups, DNS, applying DB migrations) and document them clearly.
- **Grounds UX work in named authorities + competitors.** **NN/g is her go-to UX source**; she benchmarks the product against specific comparables — **Goodreads, StoryGraph, Letterboxd, IMDb, Discogs, Fragrantica, WatchCharts/Chrono24, StockX/GOAT, Fashionphile/TheRealReal/Vestiaire, Kelley Blue Book.** Wants evaluations grounded in cited sources + competitor teardowns — but the **deliverable is actionable per-flow / customer-journey improvements**, not an abstract best-practices essay ("that's just to inform you; my priority is improving the existing UX").
- **Hands-on visual QA — she wants to *see* it.** She tests each increment in the running app and catches real bugs (a search returning zero, a card overflowing, a broken-image icon); she asks to see rendered output **inline / in-browser** (the page, a sitemap, user-flow diagrams), not just trust a green build. Judge *perceived speed* on a **production build** — `npm run dev` recompiles per route and *feels* slow/"reload-y" when prod is instant.
- **Anti-ceremony.** She questions process with no payoff ("why watch the PR? you and I are the only ones changing it") — don't add overhead (PR-watching, approvals) when there's no CI or external reviewer.

## Git / workflow (locked)
- **`main` is the single source of truth.** Sync from `main` at session start, merge back to `main` at end (see AGENTS.md "Branch & sync workflow"). Never build off old per-session branches.
- **Trust `origin/main`, not the container's local `main` (gotcha, 2026-06-26).** This container can ship a stale orphan local `main` from a retired lineage with NO common ancestor to `origin/main` (`git merge-base` returns empty, hundreds of files "differ"). Before any merge, reconcile to the remote: `git checkout main && git reset --hard origin/main`, then merge the session branch (it descends from `origin/main` and fast-forwards cleanly). NEVER `git merge --allow-unrelated-histories` here — that's the false alarm, not a real divergence.
- She prefers **automation and guardrails** so she doesn't have to remember process.
- **Recurring manual ops should become one-button automations.** She pushes back on by-hand steps (*"why do I have to do this by hand?"*) — turn them into browser-triggered automations she can self-serve with no laptop/CLI. Concrete: **DB migrations now run via a GitHub Action she triggers** (`.github/workflows/db-migrate.yml`, with an optional `repair_versions` input to reconcile the recurring "objects exist but the migration history wasn't recorded" mismatch). Prefer building this over handing her CLI commands. **Never edit a migration that's already been applied** — Supabase `db push` skips applied versions, so edits silently don't land; add a *new* migration for the delta (e.g. 0022 was edited after it ran → the new columns had to move to 0023, with `IF NOT EXISTS` guards). Verify the live columns afterward rather than assuming the run took.
- **Merging to `main` ships live** (Vercel tracks `main`), so she treats merge-to-`main` as a **deploy gate she approves explicitly.** Present finished work on the per-session branch *with verification* (`tsc`/`eslint`/`next build`/tests green + git proof), then merge on her go-ahead — often a one-word *"Merge."* Don't silently auto-merge to `main`. She'll also spot-check that it actually landed (*"is this on main and pushed to GitHub?"*) and that a specific surface really changed (*"did you change the home page copy?"*) — answer with ground truth (git log / origin sync), not assertion.
- **Expect `main` to move under you between merges** — parallel sessions land PRs constantly. Re-sync and re-check for conflicts/drift right before merging; a clean merge of someone else's PR can still pull in new work that needs your pass (see voice gate below).
- **Don't let a merge break her live app before she's applied the migration.** She applies DB migrations herself (human-gated, often a while after the code merges), so **thread new DB-dependent features behind resilient reads** (catch the missing-column error and return empty → the feature shows a placeholder/“no data yet” instead of 404-ing). Migrations ship in the same PR with clear "apply this, then it activates" notes; the app degrades gracefully until she does.
- **Migration ordering gotcha (learned 2026-06-23):** the "Apply database migrations" GitHub Action applies whatever migration files are **on `main`**. A new migration must be **merged to `main` BEFORE** she runs the Action — running it while the migration is still on a branch silently applies nothing new, and a later prod write expecting the new index/column fails confusingly. Sequence: merge → run the Action → then the prod load that depends on it. **Duplicate-number collision (learned 2026-06-28):** two parallel chats both used `0033`; `supabase db push` versions by number, so it applied one, recorded version `0033`, then failed recording the second (duplicate key). Fix = renumber the UNAPPLIED duplicate to a free slot, never touch the one already recorded.
- **Parallel background agents to keep the chat free (she asked for this 2026-06-23):** for several independent build tasks, spawn **worktree-isolated background agents** (one per task, non-overlapping files) so she keeps using the main chat. Worktrees don't get `.env.local`, so agents can't touch prod — code+tests only; prod writes stay with you/her. Verify each branch's gates yourself before recommending merge; merge stays her deploy gate.
- **Stage commits by EXPLICIT PATH, never `git add -A`/`git add .`.** The environment auto-stages other lanes' work-in-progress, so a blanket add sweeps unrelated code into your commit. This applies to subagents too (tell them in the prompt). Land doc-only work to `main` via a guarded worktree off `origin/main` that copies only your files and aborts if any non-doc path appears.
- **The env can auto-switch the working-tree branch mid-session** (sometimes onto another session's branch with stale shared files). Before landing a subagent's edits to a shared file (worklist, slate), check it isn't a regression of `origin/main`; land the unambiguous new files, re-apply shared-file deltas onto main's current version.

## Infra & deployment (how she wants it handled)
- **Hosting = Vercel** (team `darkseerbruh`); production tracks the `main` branch, so pushing to `main` is what ships code. Explain deploys in these plain terms.
- She wants **deploy/config done through AI, not dashboards** — Vercel env vars, Supabase migrations/seeds, etc. The cloud-friendly pattern she prefers: real secrets live in the **environment's secret store / config** (set once, persist across sessions, never in git), so you can script `vercel`/`supabase` actions each session instead of her pasting. Build the automation; tell her exactly which credential to drop into the env config to unlock it.
- **Never put live secrets in a committed file or in chat.** `.env*` is gitignored on purpose. When a needed credential is missing/invalid in the environment, surface that plainly (with evidence) before claiming a step is done.
- **She pastes API keys/credentials into chat to unblock herself and rotates them later.** When she does: use the credential immediately to complete the task, remind her to rotate it, and steer toward the durable fix (env store). Don't refuse or lecture — she's made the tradeoff deliberately. Watch: the Resend API key was exposed this way and rotated successfully.
- **She is done with the local/cloud split** — emphatically wants every check, setup, and deploy doable from cloud sessions ("*I am so sick of this weird local/cloud split*"). To unblock verification she's **pragmatic: she'll paste a credential and rotate it later** ("*if I have to replace keys, I will*"). So prioritize making her real state visible *now* — but still write secrets only to gitignored files / the env store, and steer her toward the durable fix (real values in the **environment secret store**, set once) rather than a per-session paste. Watch for **stub/placeholder env vars** the container ships (they shadow a real `.env.local`; `dotenv` won't override an already-set var) — a "wrong key" can actually be a hidden placeholder.
- **Analytics judgment runs as a background agent, not a dashboard she checks.** The `analyst` subagent grades behavior against strategy and surfaces *decisions* (options + a recommendation + the metric moved) to `docs/analyst-decisions.md`; canon in `docs/analyst-standard.md`. **Notification model (locked):** chat-surface always (handoff points to the decisions feed), **email digest on the weekly brief**, **phone push only when an urgent threshold trips** (rare by design). Cadence = daily light scan + weekly deep brief, run from the dedicated `luxury-catalog-analyst` worktree (kept on `main`).

## Handbag intelligence & seasonal archive (the `archivist` subagent)
- **Naming, trend, gossip, and seasonal-color questions route to the `archivist` subagent.** She owns the cultural layer and the 30-year seasonal naming archive (`docs/seasonal-archive-worklist.md` + `docs/research-drafts/seasonal-archive/`). The Data lane still owns prices.
- **Social/web data is FREE-TIER ONLY:** Firecrawl (open web + TikTok/IG discovery), the Chrome extension (login-walled depth, owner present), and Apify (unattended TikTok/IG, capped). No paid scrapers or platform APIs without her go-ahead. TikTok's official API is gated to researchers, not a path.
- **Apify is connected via `.mcp.json` (OAuth, no secret in repo).** Cap every actor run with `maxItems`; the $5/month free credits do not roll over. Prefer `clockworks/tiktok-scraper`; never the transcript add-on.
- **Never Firecrawl-scrape a brand's own `.com` season pages** (Akamai serves a 5-credit bot shell). Use reference sites, auction catalogs (Christie's/Sotheby's parse clean), or the Chrome path.
- **Chanel season codes (18C, 23P) ARE official names and writable; seasonal COLOR names vary by house.** Corrected against the brands' own sites (Chrome, 2026-06-28): Hermès + Bottega have a DEEP proprietary color lexicon; **most houses (Dior, Celine, YSL, Prada, Loewe) DO name their current colors, just plainly**; only **Gucci and Fendi** genuinely use family/descriptor labels. Confirm a color against the brand's own site before calling it official; never invent one.
- **Content ideas come from the archive.** The archivist proposes a ranked slate (`docs/research-drafts/seasonal-archive/content-ideas.md`, tagged GEO vs engagement, CTA-tied); the `copywriter` drafts; final publish/seeding stays the owner-gated Content lane.

## Product decisions she's locked
- **Brand mark = interlocked "LC" monogram** (ivory L over gold C, Playfair Display, on a dark rounded tile). It's the site favicon (`src/app/icon.svg` + `favicon.ico` + `apple-icon.png`) and her LinkedIn company logo. Glyphs are outlined to vector paths (font-independent); regen lives in the session scratchpad. Don't redesign it without her say.
- **User avatars use one component** (`src/components/Avatar.tsx`): branded gold-initial fallback, person glyph when nameless, never the browser broken-image icon. Reuse it, never re-roll an inline avatar.
- **Canonical app = the full catalog lineage** (search/identify/admin/closet/watchlist/reviews/etc.). A separate analytics-prototype lineage was merged in and retired.
- **Images:** realistic photos matter a great deal to her, **but source them — never AI-generate images of real bags** (legal: MetaBirkins/trade dress; integrity: the "never invent" rule). Strategy: **live licensed affiliate/marketplace galleries + consented UGC + first-party photos**; own a permanent base layer, use live listings to fill gaps. Full reasoning in `docs/image-strategy-research.md`. **Interim visuals must look launch-ready *now*:** text-first "isn't sustainable for launch," so ship **branded placeholder tiles everywhere** (brand wordmark + handbag silhouette — clearly an icon, not a fake photo) that **degrade gracefully** (never the browser broken-image icon), with the real-photo pipeline (`variant.image_url`, populated from a licensed source) dropping photos in automatically when sourced.
- **Authentication content = text-first + schematic diagrams, NOT photos (decided 2026-06-24).** Detailed authentication reference photos can't be licensed/bought affordably, so auth articles are **descriptive-text-led** (the precise written markers — serial formats, stitching, hardware stamps — *are* the value) plus **non-photorealistic schematic "storyboard" diagrams** that *gesture* at where a feature sits (e.g. "serial under the creed patch"). This is **distinct from and compatible with** the banned "AI-generate realistic images of real bags": diagrams must be **obviously line-art/wireframe, brand-neutral (no reproduced logos/trade dress), labeled "illustrative — not a real example,"** and only indicate placement. **Medium: prefer SVG/vector** (brand-safe, on-brand dark/gold/serif, reusable) over AI raster. Real product photos stay a separate track (licensed affiliate feeds, for value/shop content) and are NOT required to ship auth content.
- **Photo contributions (queued build):** **hybrid moderation** (trusted users auto-publish, new users queued); include **all** engagement mechanics (byline + featured hero, add-a-photo in closet, "Most Wanted Photos" board, contributor badges). Spec in `docs/handoff.md`.
- **Contributor tiers:** she loved flattering, fashion-authority naming that doubles as the **recruiting pipeline for the paid Authenticator Marketplace.** Proposed ladder: **Aficionado → Collector → Connoisseur → Authenticator → Curator** (Authenticator = the trusted/auto-publish tier). Reward quality + verification, never raw volume.
- **Don't ship features that look real but aren't — fake-door them, and measure demand.** A feature that *presents* as working but can't yet deliver (the **Authenticator Marketplace**, the **`/identify` camera tool**) *"shouldn't display in app until we have people."* Preferred pattern: keep the entry point but make it a **"Coming soon → Notify me" door** that **captures demand** (analytics event for everyone + a saved interest list = warm launch list) and **flips to the real flow automatically** once the backing exists (e.g. gate on whether any verified authenticators exist). She actively *likes* fake-doors / "I want this" buttons to validate interest before building, and to seed the launch waitlist; place them wherever the relevant intent appears.
- **Monetization moments go at the decision point, across buy / sell / rent.** On the bag page the affiliate **buy/sell/rent** links and the **want/have/had** intent belong above the fold at the value moment, not buried 600 lines down. *(Historical note: the consignor/seller referral was treated as the dominant lever at ~$1,250/referral — that assumed TRR Real Partners, now ruled out; down-weighted to ~$250 on 2026-06-24, see the reality-check sub-bullet.)* Still surface "where to **sell**" wherever a flipper signal appears (thrift-find success, the `had` closet) — the UX is good regardless of per-unit value — but the model no longer rides that one line; **buyer affiliate (traffic-bound) is the backbone.** Affiliate ops: **go direct over aggregators** (higher net rate) and prioritize the **seller/consignor** programs — **but a link-wrapping aggregator (Skimlinks) is an acceptable fallback** for merchants whose direct signup is flaky/blocked (CJ kept 404ing); one signup auto-affiliates outbound links. Use direct where it works, aggregator to fill gaps. Status of programs/keys lives in `docs/data-collection-handoff.md`. Canonical audit: `docs/monetization-moments-audit.md`.
  - **⚠️ Seller-lever reality check (call with TRR Real Partners lead, 2026-06-24):** the **TRR Real Partners consignor-referral program is NOT viable for this digital-aggregator model.** It is a high-touch, *relationship-based* program with **no trackable codes/links** for consignor referrals ("we can't be blasting out codes… untenable"; not even for influencers) — so there is no digital attribution mechanism to put on a bag page. They also specifically don't want a partner that surfaces *competing* consignment options to the user, which is exactly what LC does. The "circle back in 2 weeks" was a soft close. **TRR's buyer-side affiliate (5%/7%, direct) is a separate program and is unaffected — keep it.** Net: the `$1,250` seller lever's headline source (TRR) is gone for the automated model; the lever must be **re-sourced** (Madison Avenue Couture is niche/consignment; most others are buyer-side) **or down-weighted** in `docs/monetization-projections.md`. Don't keep citing "TRR Real Partners" as the seller channel.
  - **New/adjacent revenue streams (researched 2026-06-24).** **Rent** is now a third transaction type alongside buy/sell, mapped to the `want` intent ("rent it first"): **Vivrelle ~20%/sale via Awin** (best fit; Awin network account is held, but the Vivrelle program is **Pending** approval as of 2026-06-27 and not yet earning) and **Rent the Runway 7% via Skimlinks**; added to the model as a 5th stream. **Build the bag-page "Rent it first" CTA only AFTER the rental programs are approved** (owner's call 2026-06-24 — same "after approval, not before" pattern as Skimlinks; apply to Vivrelle in Awin first). Confirmed **no competitor has a consignor *affiliate*** (Fashionphile/Rebag/Luxury Closet all buyer-side only — TRR was the unicorn). **Personal refer-a-friend codes are not a usable revenue mechanism** (store-credit only, capped, off-brand — skip). Adjacent on-strategy streams to consider: **BriteCo insurance $10/lead via Awin** (fits `have`), **Amazon Associates** (pursuing — both care/accessories *and* actual bags via contemporary brands + Amazon Luxury Stores, so a buyer channel too; authenticity guardrail: only Amazon-fulfilled/brand-sold/Luxury Stores items, never third-party "designer" listings), and later **display ads at scale** + **B2B data/insights from the price DB** (the moat). Repair shops have no affiliate programs (direct-only). Full detail in `docs/data-collection-handoff.md` §11.
- **Affiliate network = Awin (publisher account live 2026-06).** Publisher ID `2945769`, account "Luxury Catalog, LLC". Profile complete (description/logo/site/sector/region all set); tax + payout onboarding handled directly by the owner (her tax/banking details, not tracked here — W-9 uses the GA LLC per "Who she is"). **Next:** join the resale advertiser programs under Awin's *Advertisers* tab (TRR/Fashionphile/Rebag/Vestiaire) and swap raw outbound links for Awin tracking links on bag/deal pages.
- **The bag-page value element ("what it's worth") is the synthesis of KBB + Google Shopping + Google Flights** — canonical: `docs/archive/value-module-handoff.md`. Locked principles: **show evidence before asserting** (the more it claims, the more it must be right, and data is thin); **grade a price *within* its condition tier, never across** (a cheaper-but-worn bag must not read as a deal); **year/era is a second axis** (non-monotonic — vintage can run a premium) shown like-for-like, never blended with condition; plot current **asking** listings labeled "listed" now, add realized **sold** later (sold is API-gated); when exact-variant comps are thin, **broaden scope but label it** (e.g. "across colorways"). Build it as **one adaptive component reframed by closet state** (buyer/owner/collector) over **one reusable price-axis primitive** (gauge / condition-ladder / era views), and **instrument** it so usage reveals the priority.
- **Closet model = `want` / `have` / `had`** (at variant level). One simple **"want"** list (deliberately *not* "wishlist"), with opt-in per-item price alerts as the purchase-intent signal; **`had`** = previously owned (flippers, and lets past owners still review).
- **Saving and alerting are separate but connected.** The **heart** saves to `want` (shows even logged-out; a logged-out tap prompts **create account**, not log in). The **bell** is a separate one-tap price alert (% below the typical resale price); **never auto-alert every want** (too noisy).
- **A want is breadth-able: exact / any colour family / any colourway** (`closet_item.want_spec`, migration 0035; reuse `colorFamily()`). Default = the exact variant on the page. Spec alerts match "any green" across the style's variants below each one's own median.
- **Materials = one source of truth, `src/lib/materials.ts`** (with `docs/ux/materials-taxonomy.md`). Brand-neutral (no caviar/Togo/Epsom; grained vs smooth by texture). The quiz finishes and the shop filter both import it. Never hardcode a material list anywhere else.
- **Reviews are decoupled from ownership** — review a bag you rented/borrowed/tried in-store; prompt to add it to the closet afterward; "My reviews" on the profile. Reviews should carry **structured tags + AI summaries** (Fragrantica/StoryGraph model) so they also feed search. A "verified owner" badge (review of a bag in your `have`/`had`) is a wanted trust signal.
- **UGC social layer:** Poshmark-style **public closets** (opt-in), a **"coveted closets"** ranking (how many others *want* the bags you *have*), **verified expert + authenticator profiles**, and an **expert blog/video** layer. Curated **trusted creators** + embedded **YouTube** reviews are the interim visual layer (embedding sidesteps photo copyright); **Instagram** posts added the same way via the **official authenticated Meta oEmbed Read API**, behind a **permission-first** rule (written permission before *featuring* a creator). Canonical: `docs/social-embed-strategy.md`.
- **Creator collaboration = elevate, don't just pay.** Her edge is non-cash currency — distribution, authority, GEO **bylines**, trust badges — so she'd rather *elevate* creators (feature them, let them author bylined articles as named **SMEs**, run a "5 Questions With…" interview series, co-own Pinterest boards) and wire them into **consignor-referral attribution** than buy sponsored posts. Lead non-cash; treat paid sponsorship/whitelisting as a deferred, budget-gated layer. Any value given a creator — *even featuring* — triggers an **FTC disclosure**. Canonical: `docs/creator-collaboration-strategy.md`. First picks: **Je Suis Lou** (collector) and **Redeluxe / Georgia Swain** (reseller, authentication-forward).
- **GEO is the #1 growth channel** (`docs/marketing-plan.md`): make each bag page the fact-dense source Google AI / ChatGPT cite (front-loaded answers, schema.org, sitemap, named author). Diversify affiliate across **buyer + consignor** programs. Compose answers **deterministically from real data** — never let AI invent authentication facts.
- **Variant display = Amazon PDP model.** The **style/bag is the page**; colourway / size / hardware are **selectors on it**, deduped into **dimensions** (not a flat chip list — a bag can have 2-5 sizes × 5+ colours × 2-3 hardware = dozens of variants). Switching a variant must feel **instant (no full reload)**, while **each variant keeps its own indexable `/bag/[id]` URL for GEO**.
- **Quiz = pre-signup growth loop, never gatekept.** Show the **full taste result with no account**; convert with a value prop ("don't lose it — save your profile, get your matched bags + price alerts"), stashing answers and persisting them on signup. She **hates walling results behind a signup**.
- **Taste quiz is locked to `docs/ux/taste-quiz-spec.md`** (2026-06-28). Result is a **feeling-based read** ("you feel X, the room feels Y"), never a label/archetype/city/internet-aesthetic. Three-way mark **Love it / It's fine / Not for me** (default "It's fine"); no size, fit, or price question.
- **Auth: social login + usernames.** Wants **Google OAuth** and a **custom username** captured at signup for lower-friction onboarding. **Facebook OAuth dropped** — Meta's business verification + App Review overhead is not worth the marginal adoption; Google + email/password is sufficient for launch. Don't re-add Facebook unless she explicitly requests it.
- **Brand pages = intelligence hubs, not style lists.** Surface heritage, at-a-glance stats, a price **index/ticker** + **highest recorded resale** + hero-price-over-years, the brand's most-common **colours/materials/hardware/silhouettes**, recent/upcoming releases, and a **culture & buying-experience** editorial layer that **acknowledges brand lore** (e.g. the "Hermès game" / quota-bag dynamic — "we need to acknowledge stuff like that"). Lore/editorial is **curated & sourced, never invented**; data she doesn't have (live virality, upcoming releases) is **omitted, not faked**.
- **Per-bag "The Story" module (Spotify "About this song" analog) is shipped + its design is locked.** Cited origin/design/culture tidbits + a "people behind it" strip + a self-updating market fact, on the bag page (`src/lib/bag-stories/`, `BagStory.tsx`). Design = **Option 1: category icon + gold left edge, stacked cards**. Content is code-defined, keyed by style name, sourced (never-invent); scale-up path is a `style_story` table. **Video layer is deferred to a proposal** (`docs/bag-stories-video-proposal.md`) until she greenlights the editorial curation time. (Coexists with the separate "Bag DNA" attribute-objects module from the OO-UX lane.)
- **Price data = real, multi-source, captured at EXACT spec.** Resale value is highly spec-specific — *the same bag in a different colour can sell for ~2× another* in the same leather/season — so capture **per-listing** `colour × leather × hardware × year/season × condition × inclusions`, **never a blended range per size** (*"it's just too specific to go broad"*; she'll stop a load that mixes specs). She wants the **minute details** (year produced, hardware colour, interior colour, full-set). Capture **every price signal** (current per-platform, retail-vs-resale, cross-platform low/median/high, sold/auction) **plus a ~20-year history** — reconstructed from auction archives / Wayback / cited published price tables where it isn't live. **Depth-first:** fully nail the iconic hero bags' data + UX before going wide. Canonical: `docs/data-collection-handoff.md` + `docs/data-sourcing-research.md`.
- **Prices are facts — capture pragmatically, don't over-lawyer it.** She rejected over-caution about reading public resale prices (*"why would viewing the price and displaying it the way Google does be a TOS breach?"*). So: read public listings, **store a `source_url` on every price row** (attribution + link-back, Google-style), rate-limit politely — but still **never ingest reseller photos or verbatim descriptions** (copyright) and never invent specs. The genuinely gated parts are *sold* prices + multi-platform *live* feeds (eBay Marketplace Insights, affiliate datafeeds).
- **The Shop shows only what's actually for sale, kept fresh automatically.** She wants sold/pulled listings to **drop off on their own**, not be marked by hand (chose "aggregated market, mostly automatic"). Mechanism = re-crawl + reconcile: a listing absent from a platform's latest full crawl is stamped `sold` and hidden (`listing_status`, migration 0030; `reconcile-sold.ts`; scheduled `market-refresh.yml`). **Integrity rule (locked):** a partial/broken crawl must **never** mass-retire — reconcile aborts above a 50% retire threshold and only trusts a full-crawl live snapshot. Fashionphile is automated today; TheRealReal/Vestiaire reconcile manually until they're headless-crawlable.
- **Signed-in account links live under a Profile dropdown** (Feed · Closet · Watchlist · Alerts), not separate top-level pills (owner request 2026-06-26; reverses the earlier "closet stays a top-level pill" call). Closet/watchlist/alerts stay their own routes, just reached via the dropdown; the Alerts unread badge mirrors onto the Profile trigger so it isn't buried.
- **Nav IA = grouped entry points, not a flat row.** Header (`src/components/HeaderNav.tsx`): **Shop** (clickable → `/shop`, dropdown: Deals · Coveted · Browse) · **Brands** (clickable → `/brands`, tier-grouped mega-menu + "All brands →"; mobile = single link) · **Search** (a click-into search FIELD: magnifier + muted "Search bags", distinct from the outline pills, expands to an input) · **Discover** (dropdown: Identify · Quiz · Articles) · **Profile** (signed-in dropdown, above). Footer is the **labelled superset** (Shop / Discover / You + legal) and must stay in sync. Browse-by-carry/fits slugs live in `src/lib/browse-taxonomy.ts`. **Renamed `/closets` → `/coveted-closets`** (permanent redirect in `next.config.ts`) so it can't be confused with `/closet`.
- **"Coveted" (most-coveted bags) is content-gated** until there's enough want-signal: hidden from the Shop dropdown + footer until `covetedBagsReady()` passes (see content-gating below).
- **Brand IA = one destination, filter everywhere else.** Brand is a navigable destination in exactly ONE nav slot (**Brands** → `/brands` index + mega-menu). Don't add a second brand list to Shop or any other nav item (that's the bloat she rejected). Everywhere people consume content (Shop grid, Articles), brand is an **on-page filter**, not a menu.
- **Brand lists rank by tier, never alphabetical.** Directory/menu order = **ultra-luxury → mid → thrift** (`BRAND_TIERS`/`BRAND_TIER_RANK` in `src/lib/queries.ts`), then by catalogue depth. Alphabetical stranded Bottega first, which "isn't what most people are looking for."
- **"Top styles" under a brand = most documented, not "popular."** With no per-style popularity data, surface the 3 styles with the deepest variant coverage; frame as what we know best, never a popularity claim. Homepage brands section = directory (brand heading → top 3 inline style links → "View all"), not chips.
- **Every brand list is DB-sourced and auto-updates — never hardcode brands.** Nav mega-menu, `/brands`, homepage directory, Shop facet, and Articles facet all derive from the DB, so adding a brand/style surfaces everywhere automatically. (Hardcoded brand names are allowed ONLY inside per-article chart components.)
- **Articles live at `/articles`, not `/posts`** (renamed 2026-06-26; 308 redirects in `next.config.ts` cover `/posts`, `/posts/*`, `/profile/posts`). Rule: a route's URL should match its nav label. Internal lib modules keep the `posts`/`post-actions` names.
- **The Articles index is a departmental editorial "Journal," not a flat list** (shipped 2026-06-26). A sticky department rail (Authentication / What it's worth / Comparisons / Market report) plus an on-page house filter and search; the overview previews each department with "View all," and selecting a department filters to its full list. Departments carry their calibrated-hedge frame ("markers to check, not a verdict").
- **Article departments are derived in code, not a DB column** (`src/lib/article-departments.ts`: slug-override map + keyword classifier). Reclassify an article by editing `OVERRIDES` there; no migration, no per-row backfill. (A `post.category` column is a future option, prefer it if added.)
- **Social layer is bounded — reference DB *with* community, not a social network** (canonical: `docs/engagement-strategy.md`; framing: *monetization is the end goal, engagement is the flywheel*). Judge every social feature by "builds trust + drives return visits **without** PurseForum chaos." **DON'T build:** open 1:1 DMs (fraud/moderation/leakage), free-form public posting/status updates, universal comment threads, or vanity likes — messaging only later as **structured, monetized authenticator inquiries.** **DO:** structured **activity feed** (derived events, no free text), follow/favorite closets, status profiles + **leaderboards with XP tied to value-producing UGC (never vanity)**, expert-gated articles, verified-owner badges, suggest-an-edit corrections.
- **Homepage "It bags of all time" = a ranked top-6 canon, image-free, price-led** (decided 2026-06-29): Birkin · Kelly · Chanel Classic Flap · LV Speedy · Lady Dior · LV Neverfull. **Blend lens** (an it-bag earns it on heritage/mythology AND real-world recognition, so a $19k Birkin and a $1.5k Neverfull both belong). Each cell = big gold rank numeral, house, bag, **typical resale (median) as the big number**, recorded low/high as text, one sourced hook. Dropped Coach Tabby/Swagger. The **"by price tier" framing is parked** for a future editorial/GEO module + the "Why are these the it bags of all time?" article (archivist top-20 on the content slate).
- **Homepage "Priced well today" rail = glanceable, price-led** (decided 2026-06-29): a current listing low vs the bag's recorded median, sitting beside It bags as a ⅓ sidebar. Verdict **"great price" / "good price"** sits with the price (gated to n≥5), median labeled, no sample-size clutter. It's a read on **price, not condition** (say so). Guard: drop listings >70% under median (accessory/mis-grouped data noise). Becomes condition-aware once the condition backfill lands. Code: `BestDeals.tsx` + `deals.ts`.
- **"Taste Map" is the engagement + data-moat concept she loved** — a personal taste profile that visibly fills in as users interact (completeness meter, EyeWire-style "watch it build") plus a collective taste graph; seeded by the (never-gatekept) quiz. **Recommendations: content-based over real catalogued attributes first** (explainable, honors never-invent), collaborative later — **no ML infra; the clean structured catalog is the edge.**

## Finance, money & compliance (locked stance)
- **She is deliberately cautious about handling money:** *"I don't want to mess with people's money without the education and proper backing."* Before any money-handling feature, she expects **compliance research first** and a **one-time attorney/CPA review**. "Proper backing" to her = the LLC kept separate, business insurance (general/E&O/cyber), and published **Terms of Service + Privacy Policy** as shields.
- **The money roadmap + rules live in `docs/finance-compliance.md`** (the A→D "burden ladder," keyed to one question: *do you ever take custody of other people's money?*). Locked stances from it:
  - **Today = low burden** (affiliate links + price data). She is **not handling anyone's money yet** — keep it that way until the above review happens.
  - **Marketplace (Phase C): use Stripe Connect and never custody funds**, so Stripe is the regulated money transmitter, not the LLC. Never route buyer funds into the company's own account.
  - **Subscriptions (Phase B):** PCI handled via Stripe (Checkout/Elements), build auto-renewal to **ROSCA + California ARL** (easy one-click cancel), and a **ToS is required before the first payment.**
  - **Premium "collection report" (investment value / insurance / tax)** — she's interested in this and another session has started it. Locked guardrails: value tracking is fine; **insurance = inventory-export + optional flat-fee referral, never act as an insurance agent**; **tax = a records/cost-basis export, not a calculator or advice** (handbags are collectibles → max 28%; flag the dealer-vs-investor caveat); everywhere a value appears it is an **estimate, not an appraisal**; treat holdings data as **sensitive (a theft target)**.
- **Affiliate/referral links need a visible FTC disclosure** (the `rel="sponsored"` attribute alone is not enough), price data must be **labeled an estimate and dated**, and never use **"investment" / guaranteed-return** framing. These shipped as the footer disclaimer, `/privacy` `/disclosure` `/disclaimer` pages, and per-component notes.

## Brand rules she holds (from the product brief)
- **Never invent** authentication markers/date codes/serials/hardware — leave `null` + `confidence_level: low` if unverifiable.
- **No obviously-AI-generated copy or video.** AI-*assisted* with human review is fine; never ship something that reads as AI slop (it also gets downweighted by AI search). Complements the never-AI-images rule.
- **Catalog stays free** — monetize via affiliate + (later) authenticator marketplace + premium *search capability*, never a content paywall.
- **Coach matters** — the viral thrift-store acquisition engine.
- **Mobile-first** — every page works at 375px.
- **A fact is never a subjective vote (locked 2026-06-23).** She caught that `holds_value` was a *votable* axis when value retention is a market fact computed from `price_history`. Anything measurable from catalog/price data is data-derived, never crowd-voted; opinion axes capture only lived experience (build quality, comfort, everyday wear, versatility, felt roominess). `holds_value` removed from the vote vocabulary in `src/lib/votes.ts`.
- **The catalog of styles is owner-curated top-down** (the approved backbone in `supabase/seed/research/catalog-backbone.json`). She **expands it when she chooses** — on 2026-06-23 she added **Loewe** (14th brand, #399; T1 Puzzle/Hammock/Flamenco/Gate/Goya/Amazona) after earlier excluding it, and on 2026-06-28 added **Goyard + 10 more bag houses** from the Vivrelle roster (Balenciaga, Chloé, Givenchy, Valentino, Alexander McQueen, Off-White, The Row, Jacquemus, Miu Miu; jewelry-only Vivrelle houses skipped per the handbags-only scope), so don't treat the backbone as frozen — but adding a *new brand* is her call; confirm before doing it unprompted. The backbone JSON now carries a `brandMeta` block (per-brand country + tier) so new brands seed with correct values, not hardcoded France/mid.
- **Category scope: handbags only through launch (decided 2026-06-25).** No expansion into shoes/jewelry/watches before launch. The brand name ("Luxury Catalog") already supports breadth, so expanding later needs **no rebrand**, and expanding now would dilute the topical-authority signal the GEO/affiliate engine rides on. When the time comes, the order is: **signed/branded jewelry first** (gated on handbag topical depth proven, e.g. Skimlinks accepted + GEO citations appearing), **watches year-2+ as a B2B price-data play only** (reference layer already owned by WatchCharts/Chrono24; affiliate thinnest; liability highest), **shoes skip** (StockX/GOAT already own it). Cheap pre-launch insurance only: keep the core schema category-agnostic + write launch copy as a focus, not a fence. Full reasoning + sources: `docs/category-expansion-research.md`.
- **Data-source reality (locked finding 2026-06-23):** TheRealReal JSON-LD, Fashionphile, and Vestiaire all carry **structured per-size data ONLY for hyped/contemporary models**; **mass-market brands are model-LESS in structured feeds** (Kate Spade Knott/Sam, vintage + Field/Bandit Coach come back as generic "Leather Tote", and Vestiaire's `model.name` is empty for them). Do **not** scrape free-text seller descriptions to fill model names (violates the never-invent rule).
- **Data acquisition = browser-gathering only (locked 2026-06-28).** eBay Developer Program rejected and no affiliate datafeed accepted, so capture leans entirely on browser gathering via **Firecrawl** (defeats resale-site bot-blocks, cron-able; verified on TheRealReal 2026-06-28). eBay API + affiliate feeds are closed doors; don't re-pitch them as the unlock. Detail + comprehensive source registry: `docs/data-collection-handoff.md` §0a/§0b.
- **Recurring capture has two jobs at once:** build price-over-time history AND keep current listings fresh/accurate (a vanished `listing_ref` = sold/pulled → mark it + days-on-market). One scheduled pass, both outcomes.
- **Automating capture needs three owner inputs:** `FIRECRAWL_API_KEY` in Vercel env, a credit-budget/cadence ceiling, and a go-ahead to switch the standing nightly job on. (Firecrawl `monitor` or a Vercel Cron route.)

## Brand voice & tone (locked) — canonical: `docs/voice-and-tone.md`
- **A fresh, internet-native voice** — explicitly *not* legacy-luxury hush (serif, "timeless elegance") and *not* discount breathlessness ("SCORE designer for LESS!!").
- **It's a reference/authority that also lets you shop the market — but NEVER a cart (updated 2026-06-23).** Earlier stance avoided all e-commerce language; she revised it: **shopping language is now OK** ("shop the market," "for sale," "compare prices," "best deal") because the model is **compare-and-hand-off, not a merchant** — there is **no cart and no checkout, ever**; every buy links out to the seller (Google-Shopping pattern). The authority position is kept by *comparing the whole market and handing off*, not by hiding that people are shopping. **"want" is retained** for the closet/save action (she considers "wishlist" acceptable now too, but "want" still works and stays). Affiliate links are present but framed as genuinely helpful, never pushy.
- **Rigor + precision is the mission and leads; warmth is the delivery.** Deeply informed and informational, told warmly — not "warm with facts bolted on." Priority order: (1) deeply informed/warmly told, (2) clear over clever, (3) the reassuring friend at the moment of joy ("that was a great choice, totally worth it" / "this is me in a bag"), (4) smart with your money (savvy resale), (5) **no snobbery/anti-status — a guardrail to check against, never the headline** (leading with "bags aren't status" is too defensive).
- **Persona vs. system split:** the *founder-as-face* (on-camera) may gush first-person ("I'm obsessed," "love love love"); the *brand system* (everywhere else) earns enthusiasm through specifics and **avoids empty superlatives** (*stunning, iconic, must-have*).
- **One voice, register-flexed by surface** (loosest on TikTok → tightest at authentication / price / premium-tool money-moments). It **bridges two audiences in one voice** — Gen-Z thrift-flippers *and* collectors/investors — never splitting into two brands.
- **Locked taglines:** *Carry a little art. Choose it well.* · *Know what it's worth — and what it's worth to you.*
- **Ethos:** name the honest "why" people buy (money + taste + craft + belonging) and let the reader *into the club* via real, de-gatekept knowledge — democratize what was gatekept; never pander.
- **Reference creators she loves (learn-from, don't copy):** **Je Suis Lou** (primary tonal model — informed-but-warm, anti-snobbery, transparency-as-mission, joy/memory-led value, "we don't judge"); @relaxitsonlyfashion (deadpan + deep passion); @AlexandraAnele (approachable + exacting); @juliareingoldproductions (human-on-the-other-side; the founder-persona model). Brand canon: Mr Porter ("would you say it in the pub?"), Mailchimp-minus-quirk (clear over clever), Aesop (no superlatives), Ffern (make the mechanics feel human).
- **Content channels chosen** (canonical: `docs/social-content-calendar.md`): Pinterest, Email/Newsletter, Instagram, TikTok/YouTube Shorts. Evaluate formats by **revenue proximity × solo-operator effort**; every post drives to a real page; **batch the video**; the newsletter opt-in is a known unbuilt dependency.
- **Pinterest is an *outbound* channel only** — a visual-search/shopping engine for *driving* high-intent shoppers to bag pages (Rich Pins) and for **creator collaborative/group boards**; she explicitly decided it is **not** an on-site embed source like YouTube/Instagram ("no need to consider embedding").
- **No em dashes (—), ever (locked 2026-06-23).** She called this out directly:
  em dashes are out across all user-facing copy. Use a period, comma, colon, or
  parentheses instead (rationale + replacement guidance now in `docs/voice-and-tone.md`
  §7–8). The one deliberate exception she approved: the tagline *"Know what it's
  worth — and what it's worth to you"* keeps its dash; everything else obeys the rule.
- **Apply the FULL `docs/voice-and-tone.md`, not the summary.** She asked point-blank
  whether the voice guide was being used when drafting copy; draft against the
  canonical guide (run the §8 slop sweep + the human-review checklist), don't rely
  on the condensed voice notes in this file.
- **The voice is a standing gate, not a one-time pass.** Parallel feature sessions ship user-facing copy *without* the guide and reintroduce banned patterns — a later overhaul re-added the hype word *"definitive"* in the footer and leaked an internal *"migration 0014"* string into a user-facing error. So when `main` moves or you reconcile sessions, **re-check new/changed user-facing copy against `docs/voice-and-tone.md`** and fix the drift. Mechanics that held up well: a copy-only pass touching *only* display strings (headings, labels, empty states, errors, metadata) while leaving code/structure/data untouched, **preserving every hedge & legal disclosure verbatim**, and **never inventing facts** (e.g. dropped an "what it's worth" CTA on `/identify` because the tool returns no value).

## Surfacing features on the home/entry pages (UX copy)
- **CTA copy = max 3 words** (4 only in a rare case). "Save your read, get your matches" is too long; use "Save it" / "Create account" / "See my bags." Sell the value *around* the button, never on it.
- **Name a feature one way.** The taste feature is **"Style read"** (the feature) + **"What your style says"** (the line); don't also call it "Find your taste," "Your read," or "What your style does." Pick one name per thing and use it everywhere.
- **Sign-up prompts sell the upside, never dismiss the free thing.** After a free result, don't say "it's yours either way" (reads dismissive); list what an account *adds* (keeps it, price alerts, saved matches).
- **Lead with the value prop, not a list.** On entry surfaces (home, nav), a flat list of feature links/chips is *"not a good experience."* Each use case needs a **hook that sells why it's for you** and drives traffic to the feature — motivate the click, don't just enumerate.
- **One entry per destination — dedupe overlapping flows.** If two use cases point at the same place (e.g. two chips both linking to `/identify`), **show it once** as a single combined value-prop block with copy that speaks to *both* audiences. Never surface duplicate links to the same flow.
- **Offer a few copy/positioning options to choose from.** She'll ask you to *"suggest a few options for how to best highlight"* something — give 2–3 distinct angles per item, ship a sensible default, and record the alternatives where sessions can reuse/swap them (e.g. `docs/ux/home-use-case-value-props.md`). Treat copy as A/B-swappable, not final.
- **Tiles SHOW value, they don't describe it** — the homepage goal-picker tiles demonstrate each use case (a checklist, a worth gauge, an inline quiz question), with search living ONCE in the hero. Canonical: `docs/ux/homepage-experiments.md`.
- **"What brings you in?" is 4 tiles, not 6 (locked 2026-06-26):** Is it real · Collect & invest · What's it worth · Find the bag for me. The quiz tile ends in a **Continue → CTA** (not a bare "Question 1 of N" line). **Best deals is its own full section** (a row of bags), never a single-bag tile. **Most coveted bags** left the grid (content-gated).
- **Homepage is shorter (cut 2026-06-26):** removed "Bags you might like" (recs live elsewhere), the "Explore" chip row, "Bags by what they fit", and "Bags by how they're carried". **"Stay in the loop" newsletter sits LAST, full width, one compact line.**
- **Community sections are content-gated, not faked (2026-06-26):** "What the community knows" / review leaderboards and "Most coveted" stay hidden until real signal clears a threshold. Strategy + the full list of gated surfaces: `docs/ux/content-gating-strategy.md`; gates in `src/lib/content-gates.ts`. Principle: show only when populated, never fabricate, reveal automatically.
- **Homepage layout prefs (locked 2026-06-23):** content sits in a **centered `max-w-5xl` column** (tiles/sections must not stretch edge-to-edge and strand their content on wide screens); she wants the **short/compact hero** (less vertical space, not the tall variant); the **nav divider aligns to that same content column** (full-width sticky frosted bg is fine, but the visible edge lines up).
- **Mobile must not overflow horizontally — she notices and reports it.** Nothing should spill past the viewport edge on phones. Three fixes she asked for: (1) the hero search input needs `min-w-0` so it shrinks inside the flex row instead of shoving the gold Search button off-screen (the classic flex-input trap); (2) the **newsletter signup** hit the same trap (`min-w-0` on the input + non-shrinking button, 2026-06-26); (3) the **header nav collapses into a hamburger menu on mobile** (`HeaderNav` client component — inline pills at `sm+`, stacked dropdown below) rather than a horizontally-scrolling row. Prefer collapse-to-menu over sideways-scroll for any crowded mobile row.
- **Homepage brand directory = "Brands", two columns on mobile (2026-06-26):** heading is **Brands** (not "Bags by brand"); the directory shows **two columns from 360px up**, one only on very small phones. Each house leads with its **signature icons** (owner-approved backbone order in `SIGNATURE_STYLES`, `src/lib/queries.ts`) so LV shows Neverfull and Hermès shows Birkin, then fills by catalogue depth. Honest framing: signatures, never a "most popular" claim (no popularity data).
- **Home has a "From the Journal" department shelf (2026-06-26):** `src/components/JournalShelf.tsx`, slotted above "Stay in the loop". Mirrors the Articles rail (the four departments, each with its number + calibrated-hedge frame + latest headline), routing by intent. Reuses the Journal's own helpers so it stays in sync; renders nothing until an article is published (no empty shelf).
- **Homepage H1 is a live 3-arm copy test (2026-06-26):** `home_headline` runs utility / confidence / manifesto headlines; **single variable** (subhead + search box identical across arms); **success metric = hero search-box engagement** (`home_search_engaged`). Assigned per impression, server-side, cookieless. Control/default = the utility line; the brand tagline stays in `voice-and-tone.md`, not an arm. Infra: `src/lib/experiments/home-headline.ts` + `HomeHero.tsx`; canonical: `docs/ux/homepage-experiments.md` (A2-live).
- **No fabricated numbers in UI, even illustrative ones.** Don't put sample figures (fake portfolio totals, fake prices) on tiles seen by logged-out visitors — use illustrative visuals and keep real numbers on the destination pages. Extends the never-invent rule to UI mockwork that ships.

## What she's drawn to
- "Good feels," status, and recognition mechanics for users; gamification **tied to strategy**.
- Language that makes the reader feel *let into the club* via real knowledge — **warm, precise, and unpretentious**, never ornate luxury-speak or empty superlatives (see "Brand voice & tone" above).
- Caution + honesty on legal/IP risk; grounding big decisions in research first.

## Current open work
This file deliberately tracks durable *preferences*, not volatile status. For what's
shipped vs. pending right now, read `docs/handoff.md` (it changes session to session).
